<odoo>
    <record id="view_product_form_inherit_product_ref" model="ir.ui.view">
        <field name="name">product.template.form.inherit.product.ref</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- Insert after the name field -->
            <field name="name" position="after">
                <field name="product_ref"/>
                <field name="unit_cost"/>
                <field name="unit_name_value"/>
            </field>
        </field>
    </record>
</odoo>
